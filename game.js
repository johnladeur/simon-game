var buttonColors = [
    "red",
    "blue",
    "green",
    "yellow"
]

var randomChosenColor = randomNumber + buttonColors[i]

function nextSequence(){
    var randomNumber = Math.floor(Math.random()* 4);
   console.log(randomNumber)
}